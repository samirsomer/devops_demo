apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "devops-demo.fullname" . }}-config

data:
  DEMO_APP_APP_NAME: {{ .Values.config.appName | quote }}
  DEMO_APP_LOG_LEVEL: {{ .Values.config.logLevel | quote }}
  DEMO_APP_ENVIRONMENT: {{ .Values.config.environment | quote }}
