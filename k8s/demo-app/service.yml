apiVersion: v1
kind: Service

metadata:
  name: devops-demo-service
  namespace: devops-demo
  labels:
    app: devops-demo
spec:
  selector:
    app: devops-demo-app

  ports:
  - name: http
    port: 8008
    targetPort: 8000

  type: ClusterIP
