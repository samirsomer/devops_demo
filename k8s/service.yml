apiVersion: v1
kind: Service

metadata:
  name: devops-demo-service
  namespace: devops-demo
spec:
  selector:
    app: devops-demo-app

  ports:
  - port: 8008
    targetPort: 8000

  type: ClusterIP
